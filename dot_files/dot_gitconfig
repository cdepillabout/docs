[color]
	ui = auto
[rerere]
	enabled = true
[rebase]
	autosquash = true
[push]
	# git has deprecated "tracking" and it will be called
	# "upstream" in future git releases.  "tracking" is still
	# currently working though.  Old versions of git don't
	# know about "upstream".
	default = tracking
[merge]
	defaultToUpstream = true
	ff = only
[status]
	submoduleSummary = true
[diff]
	tool = vimdiff
[alias]
	co = checkout
	br = branch
	ci = commit
